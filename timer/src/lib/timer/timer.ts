export class Timer {
    start_time: Date;
    timer_hours: number;
    timer_minutes: number;
    timer_seconds: number;
    end_time: Date;

    constructor (timer_hours: number, timer_minutes: number, timer_seconds: number) {
        this.timer_hours = timer_hours;
        this.timer_minutes = timer_minutes;
        this.timer_seconds t
    }
}